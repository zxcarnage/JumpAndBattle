namespace Utils
{
    public static class ConstValues
    {
        public const int ENEMY_HEALTH = 5;
        public const float ENTITY_SEARCH_RADIUS = 1.2f;
        public const int KEY_WIN_CONDITION = 3; //TODO: Only prototype, in real world can be flexible
    }
}